from fastapi.testclient import TestClient
from main import app


        # def test_index(self):
        #     response = client.get("/")
        #     self.assertEqual(response.status_code, 200)
        #     # assert response.
        #
        # def test_list_runs(self):
        #     response = client.get("/all_run_data")
        #     self.assertTrue(response.ok)
